import { ModulePlaceholder } from '@crm/components';

/** Placeholder until Settings has a dedicated specification. */
export default function SettingsPage() {
  return <ModulePlaceholder moduleKey="settings" />;
}
