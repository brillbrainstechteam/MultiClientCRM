/**
 * Re-exported from `domain/capabilities` so screens use the same import
 * shape as every other module (`from '../permissions'`).
 */
export { can, canCreateParallelFollowUp, providerCapabilitiesFor } from './domain/capabilities';
export type { CallingCapability } from './domain/capabilities';
